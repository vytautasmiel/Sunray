#ifndef GLOBALS_H // Use a unique name for the include guard
#define GLOBALS_H

#include "src/adapter.h" // Include the header that defines the TYPE ArduMower::Adapter

// Declare 'mower' as an external variable (defined elsewhere)
extern ArduMower::Adapter mower;

#endif // GLOBALS_H